# SubSync

Synchronize an out of sync subtitles file

## Supported formats
- `.srt`

## TODO
- support more formats
- list subtitle files in folder, select one more easily
- add command flags
  - `-f, --file`: input file
  - `-s, --seconds`: offset in seconds
  - `-v, --verbose <on|off>`: verbose (default on)
  - `-r, --replace` replace the original file
